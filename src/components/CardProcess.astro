---
interface Props {
  heading: string;
  text: string;
}

const { heading, text } = Astro.props;
---

<div class="p-1 pl-8 border-l-2 border-accent">
  <h3 class="text-[1.25rem] font-normal mb-2">{heading}</h3>
  <p class="text-gray text-[1rem] leading-relaxed">
    {text}
  </p>
</div>
